package ch3;

/**
 * Three in one
 *  Describe how you could use a single array to implement three stacks.
 *
 *  Parition the single array into 3 separate regions, A B and C.
 *  Note the start idx of each of the region. The stack on region A cannot grow past the start of region B.
 *  Stack on region B cannott grow past the start idx of region C.
 */
public class qns1 {
    public static void run() {

    }
}
